import TrackListItem from './index';
import Loading from "./index";

export default {
  title: "Loading"
}

const Template = arguments_ => <Loading {...arguments_} />

export const Primary = Template.bind({})

Primary.args ={
}